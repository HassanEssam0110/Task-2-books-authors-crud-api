{
  "name": "authors-books-api",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start:dev": "set NODE_ENV=development& nodemon server.js",
    "start:prod": "set NODE_ENV=production& node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "REST API that manages authors and their books, demonstrating relations and multiple CRUD operations.",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "joi": "^17.13.3",
    "mongoose": "^8.12.1",
    "morgan": "^1.10.0"
  }
}
